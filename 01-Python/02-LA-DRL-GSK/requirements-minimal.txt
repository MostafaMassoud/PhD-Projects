# ======================================================
# LA-DRL-GSK - Minimal Requirements (NumPy Only)
# ======================================================
#
# This is the minimal setup to run LA-DRL-GSK.
# PyTorch is NOT required for basic usage.
#
# INSTALLATION:
#   pip install -r requirements-minimal.txt
#
# USAGE:
#   from la_drl_gsk import LADRLGSK, LADRLGSKConfig
#   config = LADRLGSKConfig(dim=30, use_rl=True)  # Uses adaptive heuristic
#   result = LADRLGSK(config).optimize(objective)
#
# ======================================================

# Core (Required)
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
