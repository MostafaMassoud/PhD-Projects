# ======================================================
# LA-DRL-GSK Requirements
# ======================================================
#
# Choose the appropriate installation method:
#
# MINIMAL (NumPy only, no PyTorch):
#   pip install -r requirements-minimal.txt
#
# WINDOWS (Intel CPU with MKL):
#   python scripts/install_windows.py
#   OR manually:
#     pip install torch==2.4.1+cpu torchvision==0.19.1+cpu torchaudio==2.4.1+cpu --index-url https://download.pytorch.org/whl/cpu
#     pip install -r requirements-windows.txt
#
# macOS APPLE SILICON (M1/M2/M3):
#   python3 scripts/install_mac.py
#   OR manually:
#     pip install -r requirements-mac-m3.txt
#
# ======================================================

# Core (Required for all platforms)
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# PyTorch is OPTIONAL for basic usage
# Install based on your platform (see instructions above)
